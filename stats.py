# This module handles the calculation of statistics


def compute_stats(classmates: list[list]):
    """This function takes in a list of all the entries in the database and computes the number 
    of students who went to university, who left the state, and the top 3 universities."""
    